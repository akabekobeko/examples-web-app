
/**
 * コンテンツ領域。
 */
.l-content {
  color: #2c3e50;
}

/**
 * コンテンツのタイトル。
 */
.l-content .title {
  text-align: center;
  font-weight: normal;
  font-size: 2.5em;
}

/**
 * ツリー部分。explorer.jsx のターゲット。
 */
.l-content .explorer {
  max-width: 1024px;
  margin: auto;
}

/**
 * フォルダ内のツリー。
 */
.l-content .explorer ul {
  margin: 0;
  padding: 0 0 0 2em;
  list-style: none;
}

/**
 * フォルダ内ツリーのアイテム。
 */
.l-content .explorer li {
  padding: .2em 0;
}

/**
 * ツリー内のアイコン。
 */
.l-content .explorer [class^="icon-"],
.l-content .explorer [class*=" icon-"] {
  margin-right: .2em;
}

/**
 * フォルダの展開状態アイコン。
 */
.l-content .explorer .icon-arrow-right,
.l-content .explorer .icon-arrow-down {
  color: #95a5a6;
}

/**
 * フォルダのアイコン。
 */
.l-content .explorer .icon-folder {
  color: #3498db;
}

/**
 * ファイルのアイコン。
 */
.l-content .explorer .icon-file {
  color: #f39c12;
}
